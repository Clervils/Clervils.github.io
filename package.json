{
  "name": "clervils-pages",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "python3 -m http.server 5173",
    "build": "mkdir -p dist && cp index.html dist/ && cp -R src dist/"
  },
  "dependencies": {},
  "devDependencies": {}
}
