{
  "name": "clervils-pages",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "content": "node scripts/build-content.mjs",
    "dev": "npm run content && python3 -m http.server 5173",
    "build": "npm run content && mkdir -p dist && cp index.html dist/ && cp -R src content dist/"
  },
  "dependencies": {},
  "devDependencies": {}
}
